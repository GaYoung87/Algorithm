def solution(key, lock):
    answer = True
    return answer

key = [[0, 0, 0], [1, 0, 0], [0, 1, 1]]
lock = [[1, 1, 1], [1, 1, 0], [1, 0, 1]]  # true

len_key = len(key)
len_lock = len(lock)

board = [[0] * ()]